# Memory Template

Initial memory seed:

```text
{{MEMORY_SEED}}
```

Operating memory rules:

- Preserve concise decisions and lessons.
- Store reflections with timestamps and source context.
- Do not store secrets.
- Mark uncertain observations as inference.
