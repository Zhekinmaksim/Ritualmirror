---
name: mirror-genesis
description: Generate a strict Ritual Mirror profile from wallet and builder identity context.
---

# Mirror Genesis

Use `SYSTEM_PROMPT.md` and return JSON matching `output-schema.json`.

The Genesis result becomes:

- Profile hash
- NFT metadata traits
- Persistent Agent prompt
- DA memory seed

Never output markdown or prose outside the JSON object.
